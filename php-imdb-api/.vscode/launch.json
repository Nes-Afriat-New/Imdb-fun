{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for api",
            "type": "php",
            "request": "launch",
            "port": 9004,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}"
            },
            "log": true
        }
    ]
}
